import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    title: {

    },
    price: {
        fontSize: 18,
        fontWeight: 'bold',
        color: "#692abb"
      },
    description: {
        
    }  
});

export default styles;