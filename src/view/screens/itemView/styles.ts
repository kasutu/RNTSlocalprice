import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    title: {
        fontSize: 20,
        marginTop: 20,
        fontWeight: '700'
    },
    price: {
        fontSize: 30,
        fontWeight: 'bold',
        color: "#692abb"
      },
    description: {
        fontSize: 16,
        letterSpacing: 1
    }  
});

export default styles;